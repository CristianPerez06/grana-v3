// Re-exported from `@grana/money-logic` — the pure helpers and the
// `CategorySuggestion` transport type live in the shared package so the same
// suggestion shape ships from web actions and (eventually) mobile.
export {
  normalizeDescription,
  categoryTypeMatches,
  type CategorySuggestion,
} from '@grana/money-logic'
