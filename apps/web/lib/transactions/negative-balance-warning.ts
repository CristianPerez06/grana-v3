// Re-exported from `@grana/money-logic` — the pure soft-warning check lives
// in the shared package so web and mobile use the same implementation.
export { checkNegativeBalance, type NegativeBalanceCheck } from '@grana/money-logic'
