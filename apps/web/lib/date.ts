// Re-exported from `@grana/money-logic` so the AR date helpers live in one
// place across web and mobile. New code can import directly from the package.
export { getTodayAR, formatDateISO } from '@grana/money-logic'
