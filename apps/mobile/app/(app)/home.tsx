// Placeholder route for the "Hogar" tab.
// The tab is rendered as disabled by TabBar — it never navigates here today.
// Once the `shared` capability lands, this file becomes the real Hogar screen.
export default function HomeScreen() {
  return null
}
