// Re-exported from `@grana/money-logic` — single source of truth for AR date
// helpers across web and mobile. New code can import directly from the package.
export { getTodayAR, formatDateISO } from '@grana/money-logic'
