## 1. CLAUDE.md — workflow openspec y check automático

- [x] 1.1 Agregar a `CLAUDE.md` una sección nueva "OpenSpec — workflow obligatorio" con: (a) regla "el archive ocurre dentro de la branch como último commit, antes del merge --ff-only a main"; (b) checklist post-archivado (actualizar `Purpose`, aplicar deltas al spec plano, actualizar Modules y Repo Layout, correr `pnpm openspec:check`); (c) check pre-change-nueva (no haya otra change activa tocando la misma capability); (d) referencia a `pnpm openspec:check` como gate obligatorio antes del merge.
- [x] 1.2 Agregar al `package.json` raíz el script `openspec:check`. Implementación: dos greps secuenciales sobre `openspec/specs/` que buscan `TBD - created by archiving` y `Purpose: TBD`; cualquier match aborta con exit code 1 y mensaje a stderr. Validado: falla con TBDs presentes, pasa cuando se completan.
- [x] 1.3 Agregar a `CLAUDE.md` (en "Repo Layout" o sección adyacente) la frase de política web↔mobile: "Dos implementaciones paralelas con API idéntica garantizada por `@grana/ui-contracts`. No se comparte JSX entre web y React Native." Implementado como sub-sección dedicada "Web ↔ Mobile policy".

## 2. Sincronización CLAUDE.md / README / SUPABASE_SETUP con realidad

- [x] 2.1 Sección "Repo Layout" de `CLAUDE.md`: incluir `apps/mobile/` (con descripción "Expo, espejo funcional del web") y `packages/dashboard/`. Eliminar la frase "apps/mobile is reserved ... do not create it here". Hecho también en README.md.
- [x] 2.2 Sección "Modules" de `CLAUDE.md`: cambiar `cards` de `🚧 In progress` a completado. Agregar filas para `dashboard`, `settings`, `mobile-app-shell`, `profiles`, `i18n`, `card-networks`, `recurring-movements`, `onboarding`. Reordenar por dependencia. Limpiada también la línea huérfana de la tabla.
- [x] 2.3 Actualizar `README.md` para mencionar `apps/mobile/` en la sección de estructura del repo y en las instrucciones de desarrollo. Tabla "Scripts disponibles" extendida con `:mobile` y `eas:*`, y se agregó `pnpm openspec:check`.
- [x] 2.4 Actualizar `SUPABASE_SETUP.md` para dejar explícito que ambos apps (web y mobile) consumen el mismo proyecto Supabase. Nota agregada como callout al inicio.

## 3. Archivado de changes terminadas

- [x] 3.1 Archivar `add-onboarding-post-signup` aplicando el nuevo checklist: aplicar deltas a `openspec/specs/onboarding/spec.md` (capability nueva si corresponde) y a las modificadas (`auth`, `profiles`, `project-conventions`). Completar `Purpose` real del nuevo spec `onboarding`. Mover la carpeta a `openspec/changes/archive/2026-05-22-add-onboarding-post-signup/`. Actualizar `CLAUDE.md` Modules. Hecho: 1 ADDED + 2 MODIFIED + 2 REMOVED en auth, 2 ADDED en profiles, 1 ADDED en project-conventions, capability `onboarding` creada con 7 requirements y Purpose real. CLAUDE.md Modules ya menciona onboarding (sincronizado en Bloque A).
- [x] 3.2 Archivar `add-onboarding-mobile` después de 3.1: aplicar deltas a las capabilities `onboarding` y `auth`. Mover a `openspec/changes/archive/2026-05-22-add-onboarding-mobile/`. Hecho: 1 ADDED nuevo en auth (gate de mobile) + retag de scenarios web del Requirement del middleware; los 7 Requirements de onboarding reescritos en versión cross-platform con scenarios tagueados (web)/(mobile).
- [x] 3.3 Correr `pnpm openspec:check` y verificar que las dos changes archivadas no dejen `TBD` ni `Purpose: TBD` residuales en `openspec/specs/`. Verificado: pasa con exit 0.

## 4. Completar Purpose en specs maestros

- [x] 4.1 `openspec/specs/auth/spec.md`: Purpose real escrito (signup con OTP de 8 dígitos, login, recovery, reenvíos con cooldown, templates de email, callbacks web y mobile).
- [x] 4.2 `openspec/specs/card-networks/spec.md`: Purpose real escrito (catálogo seed, metadata visual, referencia desde tarjetas, inmutabilidad).
- [x] 4.3 `openspec/specs/cards/spec.md`: Purpose real escrito (períodos con cuatro fechas, modos novato/experto, cuotas ARS only, pago de resumen, reversión, vistas).
- [x] 4.4 `openspec/specs/dashboard/spec.md`: Purpose real escrito (landing universal, read-only, las cuatro secciones).
- [x] 4.5 `openspec/specs/i18n/spec.md`: Purpose real escrito (next-intl + cookie en web, helper RN en mobile, catálogos compartidos, scope mínimo).
- [x] 4.6 `openspec/specs/mobile-app-shell/spec.md`: Purpose real escrito (condiciones de base para Expo, resolución de paquetes del workspace, no define features de producto).
- [x] 4.7 `openspec/specs/profiles/spec.md`: Purpose real escrito (espejo de auth.users vía trigger, RLS, base para campos sumados por otras capabilities).
- [x] 4.8 `openspec/specs/project-conventions/spec.md`: Purpose real escrito (meta-spec con principio del repo-memoria, bilingüismo, branching, merge, workflow openspec, política web↔mobile).
- [x] 4.9 `openspec/specs/transactions/spec.md`: Purpose real escrito (ingresos/gastos en cash/bank, transferencias, ajustes, recurrencias, contrato Movimiento, invariantes contables).
- [x] 4.10 `openspec/specs/settings/spec.md`: el spec ya estaba alineado al código (`show-cents-toggle` + link a categorías son los únicos features del módulo). Solo se enriqueció el Purpose para mencionar explícitamente ambos features.

## 5. Fix bug financiero `new Date()`

- [x] 5.1 ~~Reemplazar `new Date().toISOString()` por `formatDateISO(getTodayAR())` en `apps/web/app/_actions/recurrences.ts:276` y `:344`.~~ **DESCARTADO — falso positivo de auditoría.** `resolved_at` es `TIMESTAMPTZ NULL` (migración 0011, línea 104), instante técnico de cuándo se resolvió la instancia. CLAUDE.md → "Accounting dates + financial timezone" aplica solo a columnas `DATE`. Para `TIMESTAMPTZ` el instante UTC actual es lo correcto; degradar a `formatDateISO(getTodayAR())` perdería la hora y rompería el contrato del campo.
- [x] 5.2 ~~Reemplazar el mismo patrón en `apps/web/app/_actions/onboarding.ts:151`.~~ **DESCARTADO — falso positivo.** `onboarding_completed_at` también es `timestamptz null` (migración 0012). Mismo razonamiento que 5.1.
- [x] 5.3 Tests con `vi.setSystemTime` agregados a `apps/web/lib/__tests__/date.test.ts` (no se creó archivo aparte; el archivo existente ya cubre `formatDateISO`). Tres casos: instante diurno AR (15:00 UTC) → fecha AR del día; ventana peligrosa (02:30 UTC del día siguiente = 23:30 AR del día actual) → fecha AR del día anterior a UTC; justo después de medianoche AR (03:00 UTC) → fecha AR del nuevo día. Los 3 pasan.
- [x] 5.4 Grep ampliado ejecutado en `apps/web/` y `packages/`. Resultados: los 3 sitios flaggeados por la auditoría (recurrences.ts x2, onboarding.ts x1) son falsos positivos (TIMESTAMPTZ). **El sitio real con bug que la auditoría NO detectó:** `apps/web/app/(app)/transactions/recurring/page.tsx:36-39` definía un `todayISO()` local que usaba `new Date()` con métodos de timezone del server y se comparaba con `rule.end_date` (columna DATE). Fix aplicado en la task 5.5 nueva.
- [x] 5.5 Reemplazar el `todayISO()` local en `apps/web/app/(app)/transactions/recurring/page.tsx` por `formatDateISO(getTodayAR())` importado de `@/lib/date`. Hecho: el helper local se eliminó; `isFinished()` ahora compara `rule.end_date` contra la fecha AR correcta independientemente del timezone del server. Typecheck y test suite (124 tests) pasan.

## 6. `@grana/ui-contracts` — tipos de props compartidos web↔mobile

- [x] 6.1 Creado `packages/ui-contracts/` con `package.json` (sin build step, `main`/`types`/`exports` → `src/index.ts`), `README.md` con la política y naming convention, y `@types/react` como devDependency para que el package pueda referenciar `ReactNode`. **No se creó `tsconfig.json` propio** porque ningún package del repo lo tiene (todos confían en `tsconfig.base.json`); seguir ese patrón evita una excepción innecesaria.
- [x] 6.2 Agregados a las 4 configs: `transpilePackages` en `apps/web/next.config.ts`, `paths` en `tsconfig.base.json`, `paths` en `apps/web/tsconfig.json`, `paths` en `apps/mobile/tsconfig.json`. También se declaró `@grana/ui-contracts: workspace:*` en `dependencies` de ambos `apps/*/package.json`.
- [x] 6.3 Definidos en `src/index.ts`: `AlertProps` (variant, title, children:ReactNode, className), `ButtonProps` (variant 5-way incluida `link`, size, loading, disabled, onPress, children, className), `CardProps` y los 5 subtipos para Header/Title/Description/Content/Footer, `InputProps` (invalid, className), `LabelProps`, `FormFieldProps` (label, error, description), `PasswordFieldProps` (extiende FormField + toggleLabelShow/Hide), `SpinnerProps` (size, className, label). Decisiones de reconciliación documentadas en el commit: `onPress` canónico para ambos lados; `link` queda en el contract aunque mobile lo trate como ghost por ahora; Spinner agrega `color` solo en mobile vía intersection; Alert children acepta ReactNode (mobile sigue soportando solo strings hoy, documentado con comentario).
- [x] 6.4 Migrados los 8 primitivos de `apps/web/components/ui/` (`alert.tsx`, `button.tsx`, `card.tsx`, `form-field.tsx`, `input.tsx`, `label.tsx`, `password-field.tsx`, `spinner.tsx`). Cada uno importa su prop type desde `@grana/ui-contracts` y extiende vía intersection con sus HTMLAttributes específicas + props propias (`asChild` en Button, `icon` en Alert, `containerClassName` en FormField). El Button web ahora soporta `onPress` además de `onClick` (mapea internamente `onClick={onPress ?? onClick}` sin romper call-sites existentes).
- [x] 6.5 Migrados los 8 de `apps/mobile/components/ui/`. Cada implementación define un tipo `Mobile<X>Props = <X>Props & <RN props extra>`. Decisión específica documentada en código: el Button mobile mantiene `title?: string` como prop legacy de compatibilidad para no romper los 14 call-sites pre-contract (`children ?? title` en el render); migración completa de los call-sites queda como deuda P2.
- [x] 6.6 Validación cruzada: `pnpm --filter web typecheck` ✓, `pnpm --filter web lint` (6 warnings preexistentes, 0 errors) ✓, `pnpm --filter web test` (124 tests) ✓, `pnpm --filter mobile typecheck` ✓.

## 7. `@grana/money-logic` — cálculo puro compartido

- [x] 7.1 Creado `packages/money-logic/` con `package.json` (sin build step), `src/{balance,cards,recurrences,index}.ts` y `README.md`. Sola dependencia runtime: `@grana/validation` (que ya trae `decimal.js`). Sin imports de Supabase, Next, fetch ni React.
- [x] 7.2 Agregado a `transpilePackages` en `apps/web/next.config.ts`, `paths` en `tsconfig.base.json`, `paths` en `apps/web/tsconfig.json`, `paths` en `apps/mobile/tsconfig.json`, `dependencies` en `apps/web/package.json` y `apps/mobile/package.json`. También se sumó al `resolve.alias` de `apps/web/vitest.config.ts` para que vitest lo resuelva en los tests.
- [x] 7.3 Extraído `calculateTransactionSums(rows, accountIds)` + tipos `BalanceCurrency`, `BalanceTransactionRow` a `packages/money-logic/src/balance.ts`. `getTransactionSums` (que llama Supabase) queda en `apps/web/lib/transactions/balance.ts`, ahora re-exportando lo puro desde el package.
- [x] 7.4 Extraído todo `apps/web/lib/cards/utils.ts` a `packages/money-logic/src/cards.ts`: `derivePeriodStatus`, `derivePeriodVariant`, `suggestNextPeriodDates`, `assignTransactionToPeriod` (genérico, preserva el tipo concreto del caller), `splitAmountIntoInstallments`, `sumMoneyValues`, `subtractMoneyValues`, helpers `formatDateISO/addDaysToISO/addMonthsToISO`. `cards/queries.ts` no se tocó (es 100% Supabase) — task 7.4 original asumía que tenía funciones puras embebidas; en realidad `cards/utils.ts` ya las tenía aisladas. `apps/web/lib/cards/utils.ts` quedó como re-export. Los tipos `CardPeriodWithPayment`, `PeriodStatus`, `PeriodVariant` ahora viven en el package con definiciones minimal (no acopladas a `Database` de Supabase); `apps/web/lib/cards/types.ts` no se tocó.
- [x] 7.5 Extraído `date.ts`, `generator.ts`, `suggestions.ts` a `packages/money-logic/src/recurrences.ts` (un solo archivo). `mapper.ts` no se mueve: importa tipos de input desde `@grana/validation` con shape específica a las server actions del módulo, y solo lo usa web. Si mobile algún día necesita el mismo mapper, sumarlo al package en una iteración futura.
- [x] 7.6 Los tests se mantuvieron en `apps/web/lib/.../__tests__/` (balance, cards/utils, cards/installments, recurrences/date, recurrences/generator, recurrences/suggestions). Importan paths relativos (`../balance`, `../utils`, etc.) que ahora son re-exports del package — los tests siguen pasando sin cambios. **Decisión documentada**: NO se movieron a `packages/money-logic/__tests__/` para mantener `pnpm --filter web test` como comando único; mover los tests al package requeriría agregar vitest como dep del package y orquestar un runner adicional desde la raíz, sin beneficio práctico mientras solo web ejecuta los tests. Si más adelante mobile sums tests sobre estas funciones, se evalúa separar.
- [x] 7.7 Validado: `pnpm --filter web typecheck` ✓, `pnpm --filter web lint` (6 warnings preexistentes, 0 errors) ✓, `pnpm --filter web test` (124 tests, incluidos los 35+ que ahora viajan a través del re-export) ✓, `pnpm --filter mobile typecheck` ✓.

## 8. Decisiones técnicas a resolver durante implementación

- [x] 8.1 Confirmado: el package se llama `@grana/money-logic`. Alternativas evaluadas y descartadas en design.md.
- [x] 8.2 Decidido: **solo props de componentes** en `ui-contracts`, sin hooks. Documentado en `README.md` del package ("Lo que NO entra acá"). Si más adelante aparece la necesidad de compartir contratos de hooks (`useEyeMask`, `useShowCents`), va en un sub-módulo `@grana/ui-contracts/hooks` con su propia change.
- [x] 8.3 Decidido: **`MoneyAmountInput` no entra** en este bloque. Mobile no tiene componente equivalente todavía; agregarlo al contract sin contraparte rompería el principio de "contract solo para pares equivalentes". Queda para una change futura cuando se construya el primitivo mobile.
- [x] 8.4 Decidido: **no se renombran los archivos mobile PascalCase a kebab-case en este pit-stop**. El cambio impactaría los imports de los 14+ call-sites existentes por puro estilo; el contract de `@grana/ui-contracts` ya garantiza la paridad de API independientemente del file naming. Queda como tarea P2 si se decide alinear todo el repo con `project-conventions`.

## 9. Cierre del pit-stop

- [x] 9.1 `pnpm openspec:check` ✓ (exit 0).
- [x] 9.2 `pnpm --filter web lint` (6 warnings preexistentes, 0 errors) ✓, `pnpm --filter web typecheck` ✓, `pnpm --filter web test` (124 tests, 13 archivos) ✓.
- [x] 9.3 `pnpm --filter mobile typecheck` ✓, `pnpm --filter mobile lint` (2 warnings preexistentes en `lib/cards/utils.ts`, 0 errors) ✓.
- [x] 9.4 Archivado en la branch como último commit antes del merge: deltas de `project-conventions` aplicados al spec maestro (los 2 nuevos Requirements integrados al flat `## Requirements`), `Purpose` ya completado en Bloque A, carpeta movida a `openspec/changes/archive/2026-05-22-chore-pit-stop-docs-and-shared-logic/`. `CLAUDE.md` Modules/Repo Layout ya están al día desde Bloque A (no requiere actualización adicional). `pnpm openspec:check` se vuelve a correr post-mv y pasa.
- [ ] 9.5 Squash de la branch a un único commit con mensaje `chore(repo): docs and shared-logic pit-stop before mobile full-feature`. **Lo ejecuta el owner manualmente desde su consola local.**
- [ ] 9.6 Merge `--ff-only` a `main`. **Lo ejecuta el owner manualmente.**
